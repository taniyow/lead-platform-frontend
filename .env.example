# Internal URL of the backend API. Server-side only (no NEXT_PUBLIC_ prefix):
# the browser never talks to the backend directly — Next.js server routes proxy to it.
BACKEND_INTERNAL_URL=http://127.0.0.1:4001
